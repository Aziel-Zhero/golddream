rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * DEPLOY TRIGGER: Forçando atualização das regras para leitura pública de fretes/cupons.
     * Versão: 1.0.5
     */

    // --- FUNÇÕES AUXILIARES ---

    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      // Verifica se o UID do usuário existe na coleção de administradores
      return isSignedIn() && exists(/databases/$(database)/documents/roles_administrador/$(request.auth.uid));
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    // --- REGRAS DE COLEÇÃO ---

    // Papéis administrativos
    match /roles_administrador/{usuarioId} {
      allow get: if isSignedIn();
      allow list, create, update, delete: if isAdmin();
    }

    // Perfis de usuários
    match /usuarios/{usuarioId} {
      allow get, list: if isOwner(usuarioId) || isAdmin();
      allow create: if isOwner(usuarioId);
      allow update: if (isOwner(usuarioId) || isAdmin());
      allow delete: if isAdmin();
    }

    // Pedidos (Qualquer um pode criar para o fluxo de WhatsApp, leitura apenas admin)
    match /pedidos/{pedidoId} {
      allow create: if true;
      allow read, update, delete: if isAdmin();
    }

    // Fretes e Cupons (Leitura pública essencial para o checkout, escrita apenas admin)
    match /fretes/{id} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /cupons/{id} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Catálogo e Configurações
    match /categorias/{categoriaId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /produtos/{produtoId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /promocoes/{promocaoId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /configuracoes/{configId} {
      allow read: if true;
      allow write: if isAdmin();
    }
  }
}